FLYTE_SYS_PATH = "_F_SYS_PATH"  # The paths that will be appended to sys.path at runtime

# Sentinel for distinguishing "argument not provided" from "explicitly set to None" in clone_with methods.
_UNSET = object()
