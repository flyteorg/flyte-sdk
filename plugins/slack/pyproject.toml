[project]
name = "flyteplugins-slack"
dynamic = ["version"]
description = "Receive Slack webhooks in Flyte."
readme = "README.md"
authors = [{ name = "Flyte Contributors" }]
requires-python = ">=3.10"
# The webhook receiver lives in flyte itself, at flyte.extras.webhooks.
# httpx (also a flyte dependency) is what notify/approval call the Slack API with.
dependencies = ["flyte", "httpx>=0.27"]

[project.optional-dependencies]
app = ["fastapi>=0.115", "uvicorn>=0.30"]
# approval.request waits on a flyteplugins-hitl event; the webhook-side handler
# needs nothing beyond the base install.
approval = ["flyteplugins-hitl"]

[build-system]
requires = ["setuptools", "setuptools_scm"]
build-backend = "setuptools.build_meta"

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "pytest-asyncio>=0.26.0",
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "httpx>=0.27",
]

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]
include = ["flyteplugins*"]

[tool.setuptools_scm]
root = "../../"

[tool.pytest.ini_options]
norecursedirs = []
log_cli = true
log_cli_level = 20
markers = []
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"

[tool.coverage.run]
branch = true

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "W", "F", "I", "PLW", "YTT", "ASYNC", "C4", "T10", "EXE", "ISC", "LOG", "PIE", "Q", "RSE", "FLY", "PGH", "PLC", "PLE", "FURB", "RUF"]
ignore = ["PGH003", "PLC0415"]

[tool.ruff.lint.per-file-ignores]
"examples/*" = ["E402"]

[tool.uv.sources]
flyte = { path = "../../", editable = true }
